doctype html
html
  head
    title Page-2
    style.
      .fieldset-auto-width {
      display: inline-block;
      }
      input[type = text]{
      width: 50%;
      padding: 12px 10px;
      margin: 15px 0;
      box-sizing: border-box;
      }
      body{
      background-color:#2b2b2b;
      color: white;
      }
  body
    h1(align='center') Lets Solve Some Riddles
    form
      center
        div(style='width: 500px;')
          fieldset.class=''
            legend(align='center') Riddle-1
            p
            center
              label What has to be broken before you can use it?
              br
              input#myText(type='text')
            p
            p(align='center')
              button
                | Submit
          h6 = message
      center
        div(style='width: 500px;')
          fieldset.class=''
            legend(align='center') Riddle-2
            p
            center
              label What is full of holes but still holds water?
              input#myText(type='text')
            p
            p(align='center')
              button
                | Submit
            h2= message2